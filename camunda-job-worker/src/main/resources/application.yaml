server:
  port: 8090
camunda:
  client:
    mode: self-managed
    auth:
      method: none
#    grpc-address: 'http://localhost:26500'
    rest-address: 'http://localhost:8080'
    enabled: true
    prefer-rest-over-grpc: true

